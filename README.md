# Mailbox administration server

This software makes a RESTful API for a database with virtual mailboxes. It allows manage
mailboxes on a mail server with the [madc](https://github.com/hugis/madc/) client
package. This database is then used by postfix and dovecot.
